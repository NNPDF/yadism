name: unit tests

on:
  push:
    branches-ignore:
      - "*docs*"

jobs:
  py37:
    uses: N3PDF/workflows/.github/workflows/python-poetry-tests.yml@main
    with:
      python-version: "3.7"
      poetry-extras: "-E mark"

  py38:
    uses: N3PDF/workflows/.github/workflows/python-poetry-tests.yml@main
    with:
      python-version: "3.8"
      poetry-extras: "-E mark"

  py39:
    uses: N3PDF/workflows/.github/workflows/python-poetry-tests.yml@main
    with:
      python-version: "3.9"
      poetry-extras: "-E mark"

  py310:
    uses: N3PDF/workflows/.github/workflows/python-poetry-tests.yml@main
    with:
      python-version: "3.10"
      poetry-extras: "-E mark"
